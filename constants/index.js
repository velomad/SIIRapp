import images from "./images";
import icons from "./icons";
import globalStyles from "./globalStyles";
import theme, { COLORS, SIZES, FONTS } from "./theme";

export { images, theme, COLORS, SIZES, FONTS, icons, globalStyles };
